const { contextBridge } = require('electron')
    // const vue = require('vue')
    // console.log(vue);
    // import { contextBridge } from "electron";
    // import { createApp } from "vue";
window.vueApp = vue
    // contextBridge.exposeInMainWorld('Vue', {
    //     createApp: (options) => console.log(options)
    // })